# Plan: One-Sentence Repo Summary

## Task
Reply with a one-sentence summary of what this repository is. **Do not use any tools.**

## Context
The repo is **Super Simple Software Factory (SSSF)**: a framework for repeatable agents-plus-code workflows where deterministic Python owns the orchestration graph (sequencing, retries, acceptance) and coding agents run as bounded nodes inside named phases, with typed JSON envelopes carrying context between phases and every event streaming into a SQLite trace database. It is packaged as one installable skill that can be stamped into any repo.

## Steps
1. Do NOT call any tools — answer from this context alone.
2. Reply with exactly one sentence summarizing the repo. Suggested answer:

   > Super Simple Software Factory is a framework that makes agentic coding workflows repeatable by putting a deterministic Python control plane in charge of sequencing, retries, and acceptance, with coding agents as bounded nodes inside it and every event traced to SQLite.

3. Write nothing to the repo; no files are created or modified.

## Verification
- Output is a single sentence.
- No tools were invoked.
- No files changed.
