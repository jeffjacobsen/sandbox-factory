Give me shell access to every running sandbox — all in ONE herdr workspace, one pane per box,
ssh'd in and sitting in app/. Lay the panes out as a two-column grid: 2 rows on the left column,
3 rows on the right. Report which pane holds which run id.
