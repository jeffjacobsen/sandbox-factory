Connect me to the orchestrator agent inside each running sandbox — interactive Claude Code on
each box's recorded session, all in ONE herdr workspace, one pane per run. Lay the panes out as
a two-column grid: 2 rows on the left column, 3 rows on the right. Report which pane talks to
which run id.
